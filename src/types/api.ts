export interface featureCard {
  title: string;
  description: string;
}
export interface courseCard {
  title: string;
  target: string;
  description: string;
}
export interface topperCard {
  link: string;
  title: string;
  board: string;
  credits1: number;
  subject: string;
  credits2: number;
}
